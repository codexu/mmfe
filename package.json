{
  "name": "mmfe",
  "version": "0.1.0",
  "description": "monorepo + mfe",
  "author": "codexu",
  "license": "MIT",
  "scripts": {
    "build": "dotenv -c production -- turbo run build",
    "dev": "dotenv -c development -- turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "tsconfig": "workspace:*",
    "turbo": "latest",
    "dotenv-cli": "latest"
  },
  "packageManager": "pnpm@8.6.10"
}
